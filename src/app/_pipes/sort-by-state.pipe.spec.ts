import { SortByStatePipe } from './sort-by-state.pipe';

describe('SortByStatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByStatePipe();
    expect(pipe).toBeTruthy();
  });
});
